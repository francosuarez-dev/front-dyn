import { HomeIcon, Ticket, CircleUser } from "lucide-react";
const listMenu = [
    {text: "Inicio", path: '/', icon: HomeIcon, private:false},
    {text: "Mi Cuenta", path: '/cuenta', icon: CircleUser, private:true},
]

export default listMenu;